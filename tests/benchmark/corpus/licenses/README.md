# Dataset Licenses

- `TESSERACT_TEST_APACHE_2.0.txt` applies to the files under `ocr_tesseract/` and is copied from the fixed
  `tesseract-ocr/test` source commit recorded in `../../annotated_sources.json`.
- `DOCLAYNET_CDLA_PERMISSIVE_1.0.txt` applies to the files under `layout_doclaynet/` and is copied from the
  fixed DocLayNet source commit recorded in `../../annotated_sources.json`.
- Files under `table_pubtables/` are distributed by PubTables-1M under CDLA-Permissive-2.0. The dataset card
  and license reference are pinned in `../../annotated_sources.json`; the license text is available at
  <https://cdla.dev/permissive-2-0/>.

Keep this directory, `../../annotated_sources.json`, and the corpus files together when redistributing the
benchmark subset.
